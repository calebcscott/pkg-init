

data-dir: /usr/share/pkg-int/etc
templates:
  default: default
  test: test.yaml
default:
  type: raw
  commands: |
    echo true
  contents:
    target:
      bin:
        -
    bin:
      -
    test:
      -
    src:
      main.c: |
        
        int main(int argc, char *argv[]) {
          print("Hello, World!");
        }
    Makefile: ""
